{{define "viewConfPage"}}
    {{template "header"}}

    <h2>Latest Configuration For {{.Name}} ({{.Address}}):</h2>
    <h4>Filename: {{.Path}} <a href="/download/{{.Path}}" download="{{.Path}}">Download File</a></h4>

    <p>
    {{range .ConfText}}
        {{.}}<br>
    {{end}}
    </p>

    {{template "footer"}}
{{end}}
