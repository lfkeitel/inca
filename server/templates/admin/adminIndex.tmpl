{{define "adminpage"}}
    {{template "header"}}

    <div class="row">
        <h2>Administration</h2>
    </div>

    <div class="row">
        <div class="col-md-2">
            <div class="list-group">
                <button type="button" class="list-group-item active" id="cpViewBtn">Connection Profiles</button>
                <button type="button" class="list-group-item" id="dtViewBtn">Device Types</button>
            </div>
        </div>
        <div class="col-md-10" id="partial-view">
        </div>
    </div>

    {{template "scripts" list "adminIndex"}}
    {{template "footer"}}
{{end}}
