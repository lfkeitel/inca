{{define "settingsPage"}}
    {{template "header"}}

    <h2>Application Configuration:</h2>
    <h4>Filename: configuration.toml</h4>

    <p class="configText">
    {{range .ConfText}}
        {{.}}<br>
    {{end}}
    </p>

    {{template "scripts"}}
    {{template "footer"}}
{{end}}
